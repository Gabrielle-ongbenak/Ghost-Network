# Database Configuration
POSTGRES_USER=ghost
POSTGRES_PASSWORD=ghost_secret
POSTGRES_DB=ghost_networks
DATABASE_URL=postgresql://ghost:ghost_secret@postgres:5432/ghost_networks

# Backend Configuration
BACKEND_API_URL=http://backend:8000/api
AUTO_SEED=true

# Apify Configuration (Optional for offline demo)
APIFY_API_TOKEN=
